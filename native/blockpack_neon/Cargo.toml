[package]
name = "blockpack_neon"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "blockpack_neon"
crate-type = ["staticlib"]

[profile.release]
opt-level = 3
lto = true
panic = "abort"
